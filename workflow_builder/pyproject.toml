[project]
name = "workflow_builder"
version = "0.1.0"
dependencies = []

[project.entry-points."agl.workflows"]
workflow_builder = "workflow_builder:workflow_builder"

[tool.agl]
requires = "agents-gl>=0.0.6"
